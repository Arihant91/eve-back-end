extend type Query { getMarketDetails: MarketGroups!
}

type MarketGroups {
    items: [GroupDetails]
}

type MarketItem {
    name: String
}

type GroupDetails {
    description : String,
    parentGroupId: Int,
    marketGroupId: Int,
    name: String,
    marketItemList: [MarketItem],
}