extend type Query { getMarketDetails: String!
}

type MarketItem {
    name: String,
    description: String,
    capacity: Float,
    groupId: Int,
    marketGroupId: Int
}

type GroupDetails {
    description : String,
    parentGroupId: Int,
    marketGroupId: Int,
    name: String,
    marketItems: [MarketItem],
    childGroups: [GroupDetails]
}

