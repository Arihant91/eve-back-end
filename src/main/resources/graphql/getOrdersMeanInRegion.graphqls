extend type Query { getOrdersMeanInRegion(regionId: Int, typeId: Int,startDate: String, endDate: String, isBuyOrder: Boolean): [OrdersMean]

}

type OrdersMean {
    regionId: Int,
    locationId: Int,
    typeId: Int,
    timeOfScraping: String,
    isBuyOrders: Boolean,
    avgPrice: Float,
    volumeRemain: Int,
    highestPrice: Float,
    lowestPrice: Float,
    orderCount: Int
}
